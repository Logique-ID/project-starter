import 'package:flutter/material.dart';

class BeritaSubscreen extends StatelessWidget {
  const BeritaSubscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text('Berita Subscreen'));
  }
}
