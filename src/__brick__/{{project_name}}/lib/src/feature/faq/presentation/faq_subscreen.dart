import 'package:flutter/material.dart';

class FAQSubscreen extends StatelessWidget {
  const FAQSubscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text('FAQ Subscreen'));
  }
}
