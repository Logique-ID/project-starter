import 'package:flutter/material.dart';

class KegiatanSubscreen extends StatelessWidget {
  const KegiatanSubscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text('Kegiatan Subscreen'));
  }
}
