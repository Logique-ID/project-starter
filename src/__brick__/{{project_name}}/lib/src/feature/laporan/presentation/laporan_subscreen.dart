import 'package:flutter/material.dart';

class LaporanSubscreen extends StatelessWidget {
  const LaporanSubscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text('Laporan Subscreen'));
  }
}
