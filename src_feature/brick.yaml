name: feature
description: Feature brick for new features
version: 0.1.1

environment:
  mason: ^0.1.2

vars:
  feature_name:
    type: string
    description: Feature name
    default: feature
    prompt: What is the feature name?
